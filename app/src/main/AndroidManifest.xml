<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BudgetWise"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.BudgetWise.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ContactActivity" />

        <activity android:name=".AgregarClienteActivity">
        </activity>

        <activity android:name=".ListaClientesActivity">
        </activity>

        <activity android:name=".AgregarProductoActivity">
        </activity>

        <activity android:name=".AgregarServicioActivity">
        </activity>

        <activity android:name=".NuevoClienteActivity">
        </activity>

        <activity android:name=".ClienteActivity">
        </activity>
        <activity android:name=".Cliente">
        </activity>

        <activity android:name=".ArticuloServiciosActivity">
        </activity>
    </application>

</manifest>